import { Canvas, Meta, Story, ArgsTable } from "@storybook/addon-docs";
import { action } from "@storybook/addon-actions";
import { ProgressBar } from "./ProgressBar";

<Meta
  t
  itle="MDX/ProgressBar"
  component={ProgressBar}
  tags={["autodocs"]}
  parameters={{ layout: "fullscreen" }}
/>

export const Template = (args) => <ProgressBar {...args} />;

# Progress Bar

Componente capaz de mostrar uma barra de progresso na tela

<Canvas>
    <Story 
    name="Progress Bar"
    args={{ 
        text: "Atividades de lógica",
        value: 10,
        min: 0,
        max: 100,
    }}>
    {Template.bind({})}
   </Story>

</Canvas>

<ArgsTable of={ProgressBar} />
