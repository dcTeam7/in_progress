import { Canvas, Meta, Story } from '@storybook/addon-docs';

import { SortName } from './SortName';

<Meta title="MDX/Checkbox" component={SortName} />

export const Template = (args) => <SortName {...args} />;

# Checkbox

Botão que ordena os alunos por nome, podendo ser ascendente ou descendente

<Canvas>
  <Story 
    name="Crescente"
    args={{ 
      label: 'Crescente',
    }}>
    {Template.bind({})}
   </Story>

   <Story 
    name="Decrescente"
    args={{ 
      label: 'Decrescente',
    }}>
    {Template.bind({})}
   </Story>
</Canvas>