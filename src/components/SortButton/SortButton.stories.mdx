import { Canvas, Meta, Story } from '@storybook/addon-docs';
import { action } from '@storybook/addon-actions';
import {SortButton} from './SortButton';

<Meta title="MDX/SortButton" component={SortButton} />

export const Template = (args) => 
<SortButton {...args}/>;

# Sort Button

Botão que possui um icone que ao clicar modifica o icone da seta para cima ou para baixo conforme State.

<Canvas>
  <Story 
    name="SortButton"
    args={{ 
      text: 'SortButton',
      stateBtn: false,
      onClick: action('clicked-button'),
    }}>
    {Template.bind({})}
   </Story>
</Canvas>
  